using BepInEx.Logging;
using BepInEx;
using System;
using System.Collections.Generic;
using System.Text;

namespace AbandonedCompanyAssets.Behaviours
{
    /*/internal class grabItem : GrabbableObject
    {
        public override void ItemActivate(bool used, bool buttonDown = true)
        {
            base.ItemActivate(used, buttonDown);
            var myLogger = new ManualLogSource("ACALogger");
            BepInEx.Logging.Logger.Sources.Add(myLogger);

            myLogger.LogDebug("I'M A FUCKING CANDLE THAT'S BEING FUCKING USED!!!!!!!!!");
        }
    }/*/
}
